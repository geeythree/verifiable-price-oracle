{
  "name": "verifiable-price-oracle",
  "version": "1.0.0",
  "description": "TEE-secured price oracle on EigenCompute with verifiable attestations on Base",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "test": "node --import tsx src/oracle.test.ts"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "dotenv": "^16.6.1",
    "fastify": "^5.1.0",
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.0",
    "tsx": "^4.21.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": [
    "eigencompute",
    "oracle",
    "tee",
    "price-feed",
    "eas",
    "base"
  ]
}
