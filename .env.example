# TEE-Provided (auto-injected by EigenCompute)
# MNEMONIC=

# Base Chain
BASE_RPC_URL=https://mainnet.base.org
CHAIN_ID=8453

# EAS (set after first schema registration — logged on startup)
# EAS_SCHEMA_UID=

# Oracle Settings
PORT=8080
PRICE_INTERVAL_MS=300000
ASSETS=ethereum,bitcoin

# Set to false to disable on-chain EAS attestations (off-chain signing still works)
ENABLE_ONCHAIN=true
